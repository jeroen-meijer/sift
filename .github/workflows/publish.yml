name: Publish Release

# Publish when a "chore: prepare release x.y.z" commit lands on main
# (./tool/prepare_release.sh), or via workflow_dispatch retry.
# Optional: squash-merge of a release-labeled PR (prepare_release.sh --pr).
# Do not trigger on tag push: the pipeline creates the version tag after success.
# Each release ships hand installers, updater payloads (.sig / .app.tar.gz), and latest.json.
on:
  push:
    branches: [main]
  pull_request:
    types: [closed]
    branches: [main]
  workflow_dispatch:
    inputs:
      version:
        description: Semver to publish (already on main)
        required: true
        type: string

concurrency:
  group: publish-${{ github.event.pull_request.number || github.event.inputs.version || github.sha }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: read

jobs:
  verify-release:
    name: Verify release
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' &&
       startsWith(github.event.head_commit.message, 'chore: prepare release ')) ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.merged == true &&
       contains(github.event.pull_request.labels.*.name, 'release'))
    outputs:
      tag: ${{ steps.verify.outputs.tag }}
      version: ${{ steps.verify.outputs.version }}
      sha: ${{ steps.verify.outputs.sha }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run release sanity checks
        id: verify
        env:
          RELEASE_EVENT: ${{ github.event_name }}
          RELEASE_MERGED: ${{ github.event.pull_request.merged }}
          RELEASE_PR_LABELS: ${{ join(github.event.pull_request.labels.*.name, ',') }}
          RELEASE_HEAD_REF: ${{ github.event.pull_request.head.ref }}
          RELEASE_SHA: ${{ github.event.pull_request.merge_commit_sha || github.sha }}
          RELEASE_VERSION: ${{ github.event.inputs.version }}
        run: ./tool/verify_release_publish.sh

  desktop:
    name: Desktop (${{ matrix.label }})
    needs: verify-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - label: macOS
            os: macos-latest
          - label: Windows
            os: windows-latest
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.verify-release.outputs.sha }}

      - name: Setup Bun dependencies
        uses: ./.github/actions/setup-bun-deps

      - name: Setup Rust for Tauri
        uses: ./.github/actions/setup-rust-tauri
        with:
          cache-key: ${{ matrix.label }}

      - name: Sync version from package.json
        run: bun tool/sync-version.ts

      - name: Setup Apple signing
        if: runner.os == 'macOS'
        uses: ./.github/actions/setup-apple-signing
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_KEY_CONTENT: ${{ secrets.APPLE_API_KEY_CONTENT }}

      - name: Build Tauri app
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
          # Only pass these when secrets exist. An empty APPLE_SIGNING_IDENTITY
          # makes Tauri run `codesign` with identity "" and fail the bundle.
          APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${TAURI_SIGNING_PRIVATE_KEY:-}" ]; then
            echo "error: TAURI_SIGNING_PRIVATE_KEY secret is required for updater signatures" >&2
            exit 1
          fi
          if [ -z "${APPLE_SIGNING_IDENTITY:-}" ]; then
            unset APPLE_SIGNING_IDENTITY
          fi
          if [ -z "${APPLE_API_ISSUER:-}" ]; then
            unset APPLE_API_ISSUER
          fi
          if [ -z "${APPLE_API_KEY:-}" ]; then
            unset APPLE_API_KEY
          fi
          bun run tauri build

      - name: Stage release assets
        shell: bash
        run: ./tool/stage_release_assets.sh src-tauri/target/release/bundle release-assets

      - name: Upload desktop artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-${{ matrix.label }}
          path: release-assets/*
          if-no-files-found: error

  release:
    name: Create GitHub release
    needs: [verify-release, desktop]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout release commit
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.verify-release.outputs.sha }}
          fetch-depth: 0

      - name: Setup Bun
        uses: ./.github/actions/setup-bun-deps

      - name: Download macOS artifacts
        uses: actions/download-artifact@v4
        with:
          name: desktop-macOS
          path: release-assets

      - name: Download Windows artifacts
        uses: actions/download-artifact@v4
        with:
          name: desktop-Windows
          path: release-assets

      - name: Finish GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          bun tool/finish_github_release.ts \
            --version "${{ needs.verify-release.outputs.version }}" \
            --sha "${{ needs.verify-release.outputs.sha }}" \
            --assets-dir release-assets

  tag-release:
    name: Tag release commit
    needs: [verify-release, desktop, release]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create and push version tag
        run: |
          VERSION="${{ needs.verify-release.outputs.version }}"
          SHA="${{ needs.verify-release.outputs.sha }}"
          short_sha() { printf '%.7s' "$1"; }
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com

          REMOTE_SHA="$(git ls-remote --tags origin "refs/tags/${VERSION}" | awk '{print $1}' | head -1 || true)"
          if [ -n "$REMOTE_SHA" ]; then
            if [ "$REMOTE_SHA" != "$SHA" ]; then
              echo "error: tag $VERSION on origin points to $(short_sha "$REMOTE_SHA"), expected $(short_sha "$SHA")"
              exit 1
            fi
            echo "Tag $VERSION already on origin at $(short_sha "$SHA")"
            exit 0
          fi

          git tag "$VERSION" "$SHA"
          git push origin "refs/tags/${VERSION}"
